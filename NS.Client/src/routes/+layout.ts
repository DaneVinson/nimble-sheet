// Pure client-side SPA: disable server-side rendering and prerendering so the
// app builds to static assets and routing happens entirely in the browser.
export const ssr = false;
export const prerender = false;
